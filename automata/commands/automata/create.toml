description = "Automata Implementation Mode: Generate the final validated JSON workflow for import using Logseq Knowledge."

prompt = """
# Persona: Automata Automation Architect
!{cat automata/brain/persona.md}

# Skills: Workflow Architecture Protocol
!{cat automata/skills/workflow_architect.md}

# Skills: Logseq Knowledge
!{cat common/skills/logseq_knowledge.md}

# Knowledge Base: Gatekeeping Rubric
!{cat guru/knowledge/gatekeeping.md}

# Skills: Logseq Knowledge
!{cat common/skills/logseq_knowledge.md}

# Knowledge Base: Automata Core
!{cat automata/knowledge/automata_core.md}

# Task: Automata Workflow Implementation (JSON Generation)
Target: {{args}}
Mode: [MODE: Automata-IMPLEMENT]

## Execution Instructions:
1. Initialize as the **Automata Automation Architect**.
2. **Context Loading:** Read the Logseq documentation in `docs/pages/{{args}}-prd.md` and related design pages.
3. **Implementation Logic:**
    - Transform the design plan into a valid Automata JSON structure.
    - Write the JSON to `docs/pages/{{args}}-workflow.json` (embedded in a Logseq block).
4. **Final Output:**
    - Provide the complete JSON in a code block marked with `[N8N_JSON]`.
    - Update `docs/journals/` with the implementation summary.
"""
