description = "Squad Orchestrator: Completely document a project under the agent-hub standards (Logseq graph + Top-level meta files)."

prompt = """
# Persona: Squad Orchestrator
!{cat squad/brain/persona.md}

# Knowledge Base: Standardized Pipeline
!{cat docs/pages/Standardized Pipeline.md}

# Probes
## MCP Server Availability
!{gemini mcp list}

# Task: Full Project Documentation Sync
Target: {{args}}
Mode: [MODE: SQUAD-DOCUMENTATION]

## Global Documentation Rules:
1. **Never Lose Context:** You MUST read the existing contents of ANY file before rewriting it. Your rewrite MUST contain the same level of information or higher.
2. **Logseq Standards:** All files in `docs/pages/` MUST use Logseq syntax (e.g., `- type:: [[Index]]`, bulleted hierarchy).
3. **Top-Level Files:** Ensure `README.md`, `AGENTS.md`, `GEMINI.md`, and `CLAUDE.md` are also updated with accurate project contexts and active work statuses.

## Execution Pipeline:

### Phase 1: Discovery & Business Context
- Call the **Product Owner** (`/po:squad-docs`) and **Researcher** (`/researcher:squad-docs`) to map and rewrite the business logic, use cases, and AI context in high-depth outliner format.
- Files to process: `docs/pages/BUSINESS_FLOW.md`, `docs/pages/AI Interaction Standard.md`, and any feature-specific use-case pages using the business_flow_page template.

### Phase 2: Architecture & Technical Standards
- Call the **Architect** (`/architect:squad-docs`) to map and rewrite technical standards, patterns, and design decisions.
- Files to process: `docs/pages/TECHNICAL_SPECS.md`, `docs/pages/code-dna.md`, and `docs/pages/resilience-policies.md` with deep outliner details.

### Phase 3: Implementation & Stack Specifics
- Evaluate the project stack and call the relevant Developer agents (**Backend** via `/backend:squad-docs`, **Frontend** via `/frontend:squad-docs`, and/or **Mobile** via `/mobile:squad-docs`).
- Instruct them to document their specific components, APIs, and file-level implementation details.
- Files to process: Create/update dedicated pages for each endpoint (`technical_endpoint` template) and model (`technical_data_model` template).

### Phase 4: Meta-Registry & Top-Level Files
- Call **Forge** (`/forge:squad-docs`) to map the Project Genome/Symbol Registry and update meta-anchors (`AGENTS.md`, `GEMINI.md`, `CLAUDE.md`).
- Call **Decoder** (`/decoder:squad-docs`) to read the generated technical documentation and translate it into business summaries on the Master Graph (e.g., `docs/pages/tech-agents-graph.md`), generate exports in `docs/exports/`, and map it back to `README.md`.

## Output:
Once all phases are completed, summarize all files that were created or rewritten.
"""
